# Core auth
JWT_SECRET=change-me
TOKEN_TTL=12h

# Public app URL used in confirmation links
PUBLIC_BASE_URL=https://milo-production-aac7.up.railway.app

# Optional SMTP for customer confirmation emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@milo.local
